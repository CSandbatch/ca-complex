@AGENTS.md

ca-complex: the deployable monorepo — five apps, shared packages, one root gate.
Human governance lives in `GOVERNANCE.md` and its siblings; agent doctrine defers
to those and never restates them.
Gate before "done": `npm run check`.
